extends layout

block content
  h1= title
  
  ul
    each recipe in recipe_list
      li 
        a(href=recipe.url) #{recipe.title} 
        |  (#{recipe.author})

    else
      li There are no books.