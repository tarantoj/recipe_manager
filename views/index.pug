extends layout

block content
  h1= title
  if locals.profile
    p Welcome to #[em Cookbook], #{locals.profile.display_name}
  else
    p Welcome to #[em Cookbook], a very basic Express website developed as a personal recipe manager

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Recipes:] #{count}
